{
  "name": "openego",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:macos": "tauri build --target x86_64-apple-darwin && tauri build --target aarch64-apple-darwin",
    "build:linux": "tauri build --target x86_64-unknown-linux-gnu && tauri build --target aarch64-unknown-linux-gnu",
    "build:windows": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
